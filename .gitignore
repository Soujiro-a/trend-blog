# 시크릿 — 절대 커밋하지 마세요
.env

# 로컬 테스트 결과물
out/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# 편집기 / 도구 로컬 설정
.claude/settings.local.json

# 세션을 옮길 때 생긴 구버전 사본 (필요 없으면 폴더째 지워도 됩니다)
/trend-blog/

# 실행 보고서는 매 실행마다 통째로 새로 쓰여서, 로컬과 GitHub Actions 양쪽에서
# 돌리면 pull 할 때마다 충돌합니다. 내용은 Actions 실행 화면의 Summary 에도
# 그대로 붙으므로 저장소에서는 추적하지 않습니다.
data/last_run.md

# data/history.json 은 반대로 반드시 커밋해야 합니다.
# 이게 없으면 매번 같은 키워드로 글을 다시 씁니다.
